server:
  port: 8082

spring:
  application:
    name: quoteservice
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG
