server:
  port: 8081

spring:
  application:
    name: jokeservice
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG
